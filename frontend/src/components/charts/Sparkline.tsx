import { cn } from "../../utils/cn";import { pathFromPoints } from "./chartUtils";export function Sparkline({values,color="#22c55e",className}:{values:number[];color?:string;className?:string}){const w=120,h=36;const max=Math.max(...values,1),min=Math.min(...values,0);const pts=values.map((v,i)=>({x:(i*w)/Math.max(values.length-1,1),y:h-((v-min)/(max-min||1))*h}));return <svg role="img" aria-label="Sparkline trend" viewBox={`0 0 ${w} ${h}`} className={cn("h-9 w-28",className)}><path d={pathFromPoints(pts)} fill="none" stroke={color} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/></svg>}
