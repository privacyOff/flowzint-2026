export function CircularProgress({value,max=100,size=96,color="#8b5cf6",label}:{value:number;max?:number;size?:number;color?:string;label?:string}){const pct=Math.max(0,Math.min(value/max,1));const r=size/2-8;const c=2*Math.PI*r;return <div className="relative inline-grid place-items-center" role="progressbar" aria-valuenow={value} aria-valuemin={0} aria-valuemax={max}><svg width={size} height={size}><circle cx={size/2} cy={size/2} r={r} fill="none" stroke="rgba(255,255,255,.1)" strokeWidth="8"/><circle cx={size/2} cy={size/2} r={r} fill="none" stroke={color} strokeWidth="8" strokeLinecap="round" strokeDasharray={c} strokeDashoffset={c*(1-pct)} transform={`rotate(-90 ${size/2} ${size/2})`} className="transition-all duration-700"/></svg><span className="absolute text-sm font-semibold">{label??`${Math.round(pct*100)}%`}</span></div>}
