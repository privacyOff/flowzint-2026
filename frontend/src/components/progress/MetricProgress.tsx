import { ProgressBar } from "../charts/ProgressBar";export function MetricProgress({label,value,suffix="%"}:{label:string;value:number;suffix?:string}){return <div className="space-y-2"><div className="flex justify-between text-sm"><span>{label}</span><span className="text-[var(--color-text-muted)]">{value}{suffix}</span></div><ProgressBar value={value} variant={value>80?"success":value>50?"warning":"danger"}/></div>}
