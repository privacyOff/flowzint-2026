import { useEffect,useRef,useState } from "react";import { cn } from "../../utils/cn";import { Button } from "./Button";
export type DropdownItem={label:string;icon?:React.ReactNode;disabled?:boolean;separator?:boolean;onSelect?:()=>void};export function Dropdown({label="Open",items=[]}:{label?:React.ReactNode;items?:DropdownItem[]}){const[open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const f=(e:MouseEvent)=>!ref.current?.contains(e.target as Node)&&setOpen(false);document.addEventListener("mousedown",f);return()=>document.removeEventListener("mousedown",f)},[]);return <div ref={ref} className="relative inline-block"><Button variant="secondary" aria-haspopup="menu" aria-expanded={open} onClick={()=>setOpen(!open)}>{label}</Button>{open&&<div role="menu" className="absolute right-0 z-50 mt-2 min-w-48 rounded-[var(--radius-md)] border border-[var(--color-border)] bg-[var(--color-surface)] p-1 shadow-[var(--shadow-lg)]">{items.map((it,i)=>it.separator?<div key={i} className="my-1 h-px bg-[var(--color-border)]"/>:<button key={i} role="menuitem" disabled={it.disabled} onClick={()=>{it.onSelect?.();setOpen(false)}} className={cn("flex w-full items-center gap-2 rounded px-3 py-2 text-left text-sm hover:bg-white/10 focus:bg-white/10 focus:outline-none disabled:opacity-40",it.disabled&&"cursor-not-allowed")}>{it.icon}{it.label}</button>)}</div>}</div>};export const DropdownExample=()=> <Dropdown items={[{label:"Settings"},{separator:true,label:""},{label:"Disabled",disabled:true}]}/>;
