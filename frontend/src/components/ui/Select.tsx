import { forwardRef } from "react";import { cn } from "../../utils/cn";export interface SelectOption{label:string;value:string;disabled?:boolean}export interface SelectProps extends Omit<React.SelectHTMLAttributes<HTMLSelectElement>,"children">{label?:React.ReactNode;options:SelectOption[];error?:React.ReactNode;helperText?:React.ReactNode;}export const Select=forwardRef<HTMLSelectElement,SelectProps>(({label,options,error,helperText,className,id,...props},ref)=><div><label htmlFor={id} className="mb-2 block text-sm font-medium">{label}</label><select ref={ref} id={id} aria-invalid={!!error} className={cn("h-10 w-full rounded-[var(--radius-md)] border border-[var(--color-border)] bg-[var(--color-surface)] px-3 text-sm focus:border-[var(--color-accent)] focus:outline-none focus:ring-2 focus:ring-[var(--color-accent)]/30",className)} {...props}>{options.map(o=><option key={o.value} value={o.value} disabled={o.disabled}>{o.label}</option>)}</select>{error?<p className="mt-1 text-xs text-[var(--color-danger)]">{error}</p>:helperText&&<p className="mt-1 text-xs text-[var(--color-text-muted)]">{helperText}</p>}</div>);Select.displayName="Select";export const SelectExample=()=> <Select label="Status" options={[{label:"Open",value:"open"}]}/>;
