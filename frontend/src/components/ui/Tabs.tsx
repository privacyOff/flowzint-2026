import { useState } from "react";import { cn } from "../../utils/cn";export interface TabItem{value:string;label:React.ReactNode;content:React.ReactNode;disabled?:boolean}export function Tabs({items,defaultValue,className}:{items:TabItem[];defaultValue?:string;className?:string}){const[value,setValue]=useState(defaultValue??items[0]?.value);const active=items.find(i=>i.value===value);return <div className={className}><div role="tablist" className="inline-flex rounded-[var(--radius-md)] border border-[var(--color-border)] bg-white/[0.04] p-1">{items.map(i=><button key={i.value} role="tab" aria-selected={value===i.value} disabled={i.disabled} onClick={()=>setValue(i.value)} className={cn("rounded px-3 py-1.5 text-sm transition",value===i.value?"bg-[var(--color-primary)] text-white shadow-[var(--shadow-glow)]":"text-[var(--color-text-muted)] hover:text-[var(--color-text)]",i.disabled&&"opacity-40")}>{i.label}</button>)}</div><div role="tabpanel" className="mt-4 animate-[fadeIn_180ms_ease-out]">{active?.content}</div></div>};export const TabsExample=()=> <Tabs items={[{value:"a",label:"A",content:"Panel A"}]}/>;
