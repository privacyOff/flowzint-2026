import { forwardRef,useId } from "react";import { cn } from "../../utils/cn";
export interface TextareaProps extends React.TextareaHTMLAttributes<HTMLTextAreaElement>{label?:React.ReactNode;helperText?:React.ReactNode;error?:React.ReactNode;prefix?:React.ReactNode;suffix?:React.ReactNode;leftIcon?:React.ReactNode;rightIcon?:React.ReactNode;}
export const Textarea=forwardRef<HTMLTextAreaElement,TextareaProps>(({className,label,helperText,error,prefix,suffix,leftIcon,rightIcon,id,required,disabled,...props},ref)=>{const auto=useId();const inputId=id??auto;return <div className="w-full"><label htmlFor={inputId} className={cn("mb-2 block text-sm font-medium",!label&&"sr-only")}>{label}{required&&<span className="text-[var(--color-danger)]"> *</span>}</label><div className={cn("flex items-start gap-2 rounded-[var(--radius-md)] border bg-white/[0.04] px-3 py-2 focus-within:border-[var(--color-accent)] focus-within:ring-2 focus-within:ring-[var(--color-accent)]/30",error?"border-[var(--color-danger)]":"border-[var(--color-border)]",disabled&&"opacity-50")}>{prefix}{leftIcon}<textarea ref={ref} id={inputId} disabled={disabled} required={required} aria-invalid={!!error} className={cn("min-h-24 flex-1 resize-y bg-transparent text-sm text-[var(--color-text)] placeholder:text-[var(--color-text-muted)] focus:outline-none",className)} {...props}/>{rightIcon}{suffix}</div>{helperText&&!error&&<p className="mt-1 text-xs text-[var(--color-text-muted)]">{helperText}</p>}{error&&<p className="mt-1 text-xs text-[var(--color-danger)]">{error}</p>}</div>});Textarea.displayName="Textarea";export const TextareaExample=()=> <Textarea label="Notes" placeholder="Add context"/>;
