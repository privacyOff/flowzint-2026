import { analytics } from "./analytics";import { dashboard } from "./dashboard";import { supportHealth } from "./supportHealth";
export const chartDatasets = { line: dashboard.conversationTrends, area: analytics.weekly, bar: analytics.topIntents.map((item) => ({ label: item.intent, conversations: item.count, escalations: item.escalationRate })), pie: analytics.intentDistribution, donut: analytics.confidenceDistribution, gauge: [{ label: "Support Health", value: supportHealth.overallHealthScore }], trend: dashboard.confidenceTrends, progress: supportHealth.healthBreakdown, heatmap: analytics.weeklyHeatmap, sparkline: analytics.trendValues };
